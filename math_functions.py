x=2.9  #if we want to round this number we have a function (riund()).
print(round(x))
x= -2.9  #if we want to change negitive number to positive number we have a function (abs()).
print(abs(x))

#if you want to write complex mathematical programs then we should inport math module.
import math
print(math.ceil(2.9))
print(math.floor(2.9))
